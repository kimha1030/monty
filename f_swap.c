#include "monty.h"

/**
 * swap - Function that prints the value at the top of the stack
 * @stack: Variable of the struct
 * @line_number: Number of line
 * Return: void
 **/

void swap(stack_t **stack, unsigned int line_number)
{
	swap_top(stack, line_number);
}
